{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "ScreenshotFullPageResult",
  "type": "object",
  "required": [
    "byte_size",
    "capture_id",
    "format",
    "height",
    "tab_id",
    "width"
  ],
  "properties": {
    "byte_size": {
      "type": "integer",
      "format": "uint64",
      "minimum": 0.0
    },
    "capture_id": {
      "description": "Opaque, session-scoped export capability. Never an agent filesystem path.",
      "type": "string"
    },
    "dialogs": {
      "type": "array",
      "items": {
        "$ref": "#/definitions/JavaScriptDialogInfo"
      }
    },
    "format": {
      "type": "string"
    },
    "height": {
      "type": "integer",
      "format": "uint32",
      "minimum": 0.0
    },
    "scope": {
      "anyOf": [
        {
          "$ref": "#/definitions/ScreenshotScope"
        },
        {
          "type": "null"
        }
      ]
    },
    "tab_id": {
      "type": "integer",
      "format": "int64"
    },
    "width": {
      "type": "integer",
      "format": "uint32",
      "minimum": 0.0
    }
  },
  "definitions": {
    "JavaScriptDialogHandledAction": {
      "description": "How the extension resolved the dialog so CDP could continue.",
      "type": "string",
      "enum": [
        "accepted",
        "dismissed"
      ]
    },
    "JavaScriptDialogInfo": {
      "description": "One observed + handled JavaScript dialog during a tool call.",
      "type": "object",
      "required": [
        "handled",
        "message",
        "sequence",
        "tab_id",
        "type"
      ],
      "properties": {
        "default_prompt": {
          "type": [
            "string",
            "null"
          ]
        },
        "handled": {
          "$ref": "#/definitions/JavaScriptDialogHandledAction"
        },
        "has_browser_handler": {
          "type": [
            "boolean",
            "null"
          ]
        },
        "message": {
          "type": "string"
        },
        "sequence": {
          "description": "Monotonic per-tab sequence for ordering within a session.",
          "type": "integer",
          "format": "uint64",
          "minimum": 0.0
        },
        "tab_id": {
          "type": "integer",
          "format": "int64"
        },
        "type": {
          "$ref": "#/definitions/JavaScriptDialogType"
        },
        "url": {
          "type": [
            "string",
            "null"
          ]
        }
      }
    },
    "JavaScriptDialogType": {
      "description": "Native JS dialog kind reported by CDP.",
      "type": "string",
      "enum": [
        "alert",
        "confirm",
        "prompt",
        "beforeunload"
      ]
    },
    "ScreenshotScope": {
      "type": "string",
      "enum": [
        "follow",
        "current"
      ]
    }
  }
}
