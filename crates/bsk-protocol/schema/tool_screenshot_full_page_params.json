{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "ScreenshotFullPageParams",
  "description": "Scroll the session's active web page from top to bottom. PNG bytes are exported in bounded chunks through `tool.screenshot_read`, then released.",
  "type": "object",
  "required": [
    "session_id"
  ],
  "properties": {
    "scope": {
      "description": "Follow appended content (default), or capture the initial document height.",
      "anyOf": [
        {
          "$ref": "#/definitions/ScreenshotScope"
        },
        {
          "type": "null"
        }
      ]
    },
    "session_id": {
      "type": "string"
    },
    "tab_id": {
      "type": [
        "integer",
        "null"
      ],
      "format": "int64"
    },
    "timeout_ms": {
      "description": "Capture and PNG encoding deadline; defaults to 120000 milliseconds.",
      "type": [
        "integer",
        "null"
      ],
      "format": "uint32",
      "minimum": 0.0
    }
  },
  "definitions": {
    "ScreenshotScope": {
      "type": "string",
      "enum": [
        "follow",
        "current"
      ]
    }
  }
}
