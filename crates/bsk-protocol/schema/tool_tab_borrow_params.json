{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "TabBorrowParams",
  "description": "Params for `tool.tab_borrow`. Moves a *user* tab into the requesting session's Agent Window, recording its original window / index so `tab_return` (or session_stop) can put it back.",
  "type": "object",
  "required": [
    "session_id",
    "tab_id"
  ],
  "properties": {
    "confirm": {
      "description": "Legacy input, ignored. Browser settings decide whether confirmation is required.",
      "type": [
        "boolean",
        "null"
      ]
    },
    "confirmation_timeout_ms": {
      "description": "Maximum time to wait for user confirmation (default 60 seconds).",
      "type": [
        "integer",
        "null"
      ],
      "format": "uint32",
      "minimum": 0.0
    },
    "session_id": {
      "type": "string"
    },
    "tab_id": {
      "type": "integer",
      "format": "int64"
    }
  }
}
